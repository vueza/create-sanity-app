/** @type {import("syncpack").RcFile} */
export default {
  dependencyTypes: ["**"],
  semverGroups: [
    {
      packages: ["**"],
      dependencyTypes: ["**"],
      dependencies: ["**"],
      range: "",
    },
  ],
};
